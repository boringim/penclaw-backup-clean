# 模型安全配置 - 强制使用免费模型

**⚠️ 重要声明**: 本 workspace 及其所有衍生 workspace 必须使用免费或本地模型，禁止使用付费模型。

## 当前配置

- **OpenRouter 模型**: `openrouter/stepfun/step-3.5-flash:free` (✅ 免费)
- **Local 模型**: `codex-local/gpt-5.4` (✅ 本地，无费用)
- **Gemini 本地**: `local-gemini` (✅ 本地，无费用)

## 禁止的付费模型

- ❌ Sonar Pro (perplexity-llama-3.1-sonar-large-128k-online)
- ❌ GPT-4o / GPT-4o-mini
- ❌ Claude 付费系列 (claude-3-5-sonnet, claude-3-opus)
- ❌ 任何非 :free 后缀的 OpenRouter 模型

## 防护措施

1. 环境变量设置 (优先级最高):
   - `OPENROUTER_DEFAULT_MODEL=openrouter/stepfun/step-3.5-flash:free`
   - 或使用本地模型避免外网调用

2. 配置文件检查 (workspace 根目录):
   - `.env` 中不设置付费模型
   - `auth-profiles.json` 只配置免费模型

3. 运行时检查:
   - 每次会话开始前读取本文件
   - 检测到付费模型使用立即警告并停止

## 安全审计日志

- 2026-03-13: 初始配置，强制免费模型

---

**如果发现使用付费模型，请立即：**
1. 停止当前任务
2. 检查环境变量和配置文件
3. 更新本文件记录
4. 通知管理员
